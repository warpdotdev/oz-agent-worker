name: TMP Validate org-wide-checks (consumer repo)
on:
  pull_request:
    branches:
      - test/validate-org-wide-checks-consumer

jobs:
  call-org-wide-checks:
    uses: warpdotdev/warp-github-actions/.github/workflows/org-wide-checks.yml@fix/org-wide-checks-diff-scoped-secrets
